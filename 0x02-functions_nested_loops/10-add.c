#include "main.h"
/**
 * add - adds two integers and returns the result
 * @y: an integer
 * @z: an integer
 * return: returns the addition of given parameter
 */
int add(int y, int z)
{
	return (y + z);
}
